const router = require('express').Router();

/*
    You can define your routes here. For example, if you have a search function for your MongoDB you could do something like 
    
    const searchRoute = require('./search');
    router.use('/search', searchRoutes);

    after defining your search route (and probably making a controller so that you have something to return on a request).
*/



module.exports = router;

